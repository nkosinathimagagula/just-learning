{
  "name": "demo",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc && node -e \"require('fs').copyFileSync('package.json', 'dist/package.json')\"",
    "deploy": "pnpm build && terraform init && terraform apply -auto-approve",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.1127.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.163",
    "@types/node": "^26.5.0",
    "typescript": "^7.0.2"
  }
}
